import 'package:flutter/material.dart';

sealed class SearchAppConstants {
  //colors for app
  static const Color primaryColor = Color.fromARGB(255, 20, 99, 245);
  static const Color whiteColor = Colors.white;
  static const Color primaryTextColor = Color.fromRGBO(33, 24, 20, 1);
  static const Color placeholderColor = Color.fromRGBO(191, 191, 191, 1);

  static const String splashScreenText = 'Search App';
}
